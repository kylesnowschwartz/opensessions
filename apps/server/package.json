{
  "name": "@opensessions/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/main.ts",
  "types": "src/main.ts",
  "scripts": {
    "start": "bun run src/main.ts",
    "dev": "bun --watch run src/main.ts"
  },
  "dependencies": {
    "@opensessions/runtime": "workspace:*",
    "@opensessions/mux-tmux": "workspace:*",
    "@opensessions/mux-zellij": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
